Step 1: design cache for single machine.
Step 2: extend to many machine.
  opt 1. each machine has its own cache
  opt 2. each machine stores a segment of the whole cache. segment is hashed by  hash(query)%N

Step 3: updating cache result.
